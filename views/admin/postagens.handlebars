<h2>Lista de Postagens:</h2>
<hr>
<a href="/admin/postagens/add">
  <button class="btn btn-success mb-3">Nova Postagem</button>
</a>
{{#each postagens}}
  <div class="card mb-3">
    <div class="card-body">
      <h5 class="card-title">Título: {{titulo}}</h5>
      <p class="card-text">Slug: {{slug}}</p>
      <p class="card-text">Categoria: {{categoria.nome}}</p>
      <p class="card-text">Data: {{data}}</p>
      <a href="/admin/postagens/edit/{{_id}}"><button class="btn btn-success mt-3">Editar Postagem</button></a>
      <form action="/admin/postagens/deletar" method="POST" style="display: inline;">
  <input type="hidden" name="id" value="{{_id}}">
  <button class="btn btn-danger mt-3" type="submit">
    Deletar Postagem
  </button>
</form>
    </div>
  </div>  
{{/each}}
